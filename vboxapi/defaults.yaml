# -*- coding: utf-8 -*-
# vim: ft=yaml
---
vboxapi:
  pkg:
    name: vboxapi
  rootgroup: root
  config: '/etc/vboxapi'
  service:
    name: vboxapi
  subcomponent:
    config: '/etc/vboxapi-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
